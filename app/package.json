{
  "name": "modesh-watchtower",
  "version": "1.0.6",
  "description": "NodeSh Watchtower dApp",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "pack:linux": "rm -rf ./nodesh-watchtower-linux-x64 && electron-packager ./ nodesh-watchtower && electron-installer-debian --src ./nodesh-watchtower-linux-x64/ --dest ../packages/ --arch amd64 --icon dist/logo.png"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "electron": "^9.0.5",
    "electron-installer-debian": "^3.0.0",
    "electron-packager": "^14.2.1",
    "nodemon": "^2.0.4"
  }
}
